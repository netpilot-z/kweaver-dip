# 显示规范

## 回显模板

```
### <标题>（<类型>）
说明：
- <要点>
下一步：<动作或确认短语>
```

标题用自然中文。确认类必须含可执行选项。

## 展示策略

- 默认仅输出摘要（数量、状态、风险、下一步）
- 同一轮只用一种主格式（文本摘要 / 表格 / YAML 三选一）
- 详情仅在用户明确请求时输出
- skill 内部产物静默计算，仅最终摘要对外

## 字段显示词典

| 内部编码 | 中文 | | 内部编码 | 中文 |
|----------|------|-|----------|------|
| `keep` | 保留 | | `mapped` | 已映射 |
| `add` | 新增 | | `waived` | 已豁免 |
| `rename` | 重命名 | | `blocked` | 待处理 |
| `merge` | 合并 | | `core_business` | 核心业务 |
| `drop_candidate` | 建议移除 | | `view_derived` | 视图衍生 |

## 约束

- 禁止 emoji
- ID 不截断
- 百分比保留 1 位小数
- Description 仅写稳定业务语义，映射不确定信息放独立字段
